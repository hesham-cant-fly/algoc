algorithme test

variable
  a: entier // int type
  b: entier

début
  a := 1
  b := 1
  dbg a
  dbg b

  dbg a <> b

  // dbg vrai et vrai // => vrai
  // dbg vrai et faux // => faux
  // dbg faux et vrai // => faux
  // dbg faux et faux // => faux
  // dbg vrai ou vrai // => vrai
  // dbg vrai ou faux // => vrai
  // dbg faux ou vrai // => vrai
  // dbg faux ou faux // => faux
fin
